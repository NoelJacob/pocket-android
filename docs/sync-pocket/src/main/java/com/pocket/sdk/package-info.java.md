# sync-pocket/src/main/java/com/pocket/sdk/package-info.java

## What this is

This is the package overview doc: the front door explaining what the classes in this package are collectively for. It carries no runtime code, only documentation the IDE and Javadoc render.

## How it fits

It describes the package its sibling files live in; newcomers should read it before any single class in that package.

## Key pieces

- (Small file with no top-level declarations matched; read it directly — it is likely constants, aliases, or wiring.)

Classes in this package:

- `Pocket.java`
- `PocketExtensions.kt`

## Junior notes

- Read the file top to bottom once; it is small and its declaration order follows its logic.

Names you will also see here: `Pocket`.
