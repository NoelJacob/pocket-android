# sync-pocket/src/main/graphql/v3.graphqls

## What this is

This is a GraphQL schema + operations file for the sync layer: it declares the fields and shapes the app asks the server for. Apollo codegen reads it at build time and generates the typed request/response classes the sources use. Key entries include `Annotation`, `Account`, `EmailAlias`, `getuser`, `guid`, `Signup`, `OAuthAuthorize`, `OAuthSsoauth`, `validateEmail`, `getAfterLogin`, `OAuthRequestMeta`, `PremiumGift`.

## How it fits

It is compiled by the Apollo Gradle plugin into generated models consumed by ClientApiSource (GraphQL transport) and, for v3 shapes, V3Source. In fdroid builds the local Express server reimplements these operations as REST under `/v3/` by reading the operation names, so the `.graphqls` files stay untouched while the wire format changes.

## Key pieces

Each entry below is an operation or schema type codegen turns into a typed class; the local server reimplements operation names as REST routes.

- `Annotation` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `Account` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `EmailAlias` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `getuser` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `guid` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `Signup` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `OAuthAuthorize` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `OAuthSsoauth` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `validateEmail` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `getAfterLogin` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `OAuthRequestMeta` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `PremiumGift` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `SignInWithAppleWebFlow` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `get` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `SearchMeta` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `SearchItem` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `SearchMatch` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `ClientSettings` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `ListenSettings` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- `fetch` — declared operation/type in this file; generated classes carry this name into ClientApiSource call sites.
- …and 136 more entries (this is a large schema file; search it by operation name).

## Junior notes

- Never hand-edit generated Apollo classes; change this file and rebuild so codegen stays the source of truth.
- Operation names are the contract with the local server (`POST /v3/<opname>`); renaming one means updating the server too.
