# Pocket/src/main/res/drawable/splash_window_bg.xml

## What this is

Vector/shape drawable (`<layer-list>`): Solid window background color layer behind the splash logo. XML drawables scale cleanly on every density, unlike PNGs.

## How it fits

Referenced as `@drawable/splash_window_bg`; used by splash.xml, UserManager.java.
