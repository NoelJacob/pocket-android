# Pocket/src/main/res/drawable/listen_scrub_button.xml

## What this is

Vector/shape drawable (`<vector>`): Draggable scrub knob for the listen progress bar. XML drawables scale cleanly on every density, unlike PNGs.

## How it fits

Referenced as `@drawable/listen_scrub_button`; used by ListenPlayerView.java.
