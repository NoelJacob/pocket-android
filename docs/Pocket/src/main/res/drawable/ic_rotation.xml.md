# Pocket/src/main/res/drawable/ic_rotation.xml

## What this is

Vector/shape drawable (`<animated-selector>`): Animated rotation-lock toggle (checked/unchecked selector). XML drawables scale cleanly on every density, unlike PNGs.

## How it fits

Referenced as `@drawable/ic_rotation`; used by PktRotationLockView.java.
