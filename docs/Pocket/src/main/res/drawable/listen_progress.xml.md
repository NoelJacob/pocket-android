# Pocket/src/main/res/drawable/listen_progress.xml

## What this is

Vector/shape drawable (`<layer-list>`): Scrub-bar progress drawable for the listen player. XML drawables scale cleanly on every density, unlike PNGs.

## How it fits

Referenced as `@drawable/listen_progress`; used by view_listen.xml, view_listen_player.xml.
