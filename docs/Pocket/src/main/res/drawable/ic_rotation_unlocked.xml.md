# Pocket/src/main/res/drawable/ic_rotation_unlocked.xml

## What this is

Vector/shape drawable (`<vector>`): Unlocked-rotation icon state. XML drawables scale cleanly on every density, unlike PNGs.

## How it fits

Referenced as `@drawable/ic_rotation_unlocked`; used by ic_rotation.xml.
