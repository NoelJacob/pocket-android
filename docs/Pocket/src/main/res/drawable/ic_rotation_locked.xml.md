# Pocket/src/main/res/drawable/ic_rotation_locked.xml

## What this is

Vector/shape drawable (`<vector>`): Locked-rotation icon state. XML drawables scale cleanly on every density, unlike PNGs.

## How it fits

Referenced as `@drawable/ic_rotation_locked`; used by ic_rotation.xml.
