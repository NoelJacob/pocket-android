# Pocket/src/main/res/drawable/splash.xml

## What this is

Vector/shape drawable (`<layer-list>`): Cold-start splash: layers the window background with the centered Pocket logo. XML drawables scale cleanly on every density, unlike PNGs.

## How it fits

Referenced as `@drawable/splash`; used by themes.xml.
