# Pocket/src/main/res/drawable-xxxhdpi/pkt_prem_purchase_reader.png

## What this is

Bitmap artwork (PNG): Premium upsell art: premium reader panel. This file is the `xxxhdpi` variant: Android picks the closest density/night/size qualifier at runtime, so the same `@drawable/pkt_prem_purchase_reader` reference resolves here on matching devices. Binary asset: role and provenance only, never contents.

## How it fits

Referenced as `@drawable/pkt_prem_purchase_reader`; resolved through the base `@drawable/{stem}` name alongside its sibling density variants. Checked-in binary with no vector source in the repo; to change the art, replace every density variant.
