# Pocket/src/main/res/raw/silence04s.mp3

## What this is

Bundled audio asset: 0.4s silent MP3 used to keep the text-to-speech audio focus chain alive between utterances.

## How it fits

Played via `MediaPlayer` with `R.raw.silence04s` by `AndroidMediaPlayer`; shipped as a binary with no in-repo source.
