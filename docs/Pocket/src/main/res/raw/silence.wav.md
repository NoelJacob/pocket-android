# Pocket/src/main/res/raw/silence.wav

## What this is

Bundled audio asset: Short silent WAV used to keep the text-to-speech audio focus chain alive between utterances.

## How it fits

Played via `MediaPlayer` with `R.raw.silence` by `ListenMediaService`; shipped as a binary with no in-repo source.
