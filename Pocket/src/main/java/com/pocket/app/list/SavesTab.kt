package com.pocket.app.list

// ponytail: local replacement for deleted analytics SavesTab; ARCHIVE/SAVES drives list behavior.
enum class SavesTab(val value: String) {
    SAVES("saves"),
    ARCHIVE("archive")
}
